function preload(){
  // preload assets: Schriftarten etc. -> alles was vorher geladen werden muss
}

function setup() {
  // wird nur ein Mal aufgeführt
  createCanvas(400, 400);
}

function draw() {
  // Rekursiv -> loop z.B. für Animationen (Musik ...) -> kann angehalten werden noLoop();
  background(15, 100, 67);
}
